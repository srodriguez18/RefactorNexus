{
  "name": "legacy-nexus",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:api": "pnpm --filter @legacy-nexus/api dev",
    "dev:web": "pnpm --filter @legacy-nexus/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
